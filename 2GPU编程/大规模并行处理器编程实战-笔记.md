参考[《大规模并行处理器编程实战》第一部分学习笔记 - 知乎](https://zhuanlan.zhihu.com/p/916336354)

## 只看1-4章

✅ **第1章：Introduction（必看）**

**你会学到：**

- GPU 和 CPU 的本质区别
- 什么是“并行计算”
- 为什么 GPU 适合做大规模计算

👉 这一章的目标不是细节，而是建立一个直觉：

> GPU = 用大量简单核心同时干活

------

✅ **第2章：Heterogeneous Data Parallel Computing**

**这是最关键的一章之一！**

**重点理解：**

- Host（CPU） vs Device（GPU）
- Kernel 是什么
- Thread / Block / Grid 的关系

👉 看到这里你应该能回答：

- “一个 CUDA 程序是怎么跑起来的？”
- “线程是怎么组织的？”

------

✅ **第3章：Basic CUDA Programming（选看）**

**你不需要细看代码细节，但要抓住流程：**

- 数据从 CPU → GPU
- GPU 执行 kernel
- 结果返回

👉 目标：

> 知道 CUDA 程序“长什么样”

------

✅ **第4章：Memory and Data Locality（非常重要）**

**GPU 编程的核心难点就在这！**

你需要理解：

- Global memory（慢）
- Shared memory（快）
- Register（更快）

👉 关键认知：

> GPU 性能 = 很大程度取决于“你怎么用内存”

------

🧠 你这一阶段的目标（非常关键）

看完这些内容，你应该能回答这几个问题：

1. GPU 和 CPU 有什么本质区别？
2. CUDA 里 thread / block / grid 是什么关系？
3. 一个 CUDA 程序执行流程是怎样？
4. 为什么 GPU 内存访问很重要？

## Chapter2,异构数据并行计算*

### 2.3向量加法核函数

两个后缀来表示主机（`_h`）和设备（`_d`）

向量加法：

1.在GPU复制变量，将数据从host搬到device

2.在GPU中计算

3.将GPU中的结果给CPU，将数据从device搬到host

### 2.4设备全局内存和数据搬运

cudaMalloc，cudaFree，size参数对应的单位是字节

![image-20260422145749693](D:\proj\LittelWidgets\2GPU编程\大规模并行处理器编程实战-笔记.assets\image-20260422145749693.png)

![image-20260422153917605](D:\proj\LittelWidgets\2GPU编程\大规模并行处理器编程实战-笔记.assets\image-20260422153917605.png)

CUDA C = C语言 + 少量扩展

### 2.5核函数和线程

核函数：在并行阶段，所有线程将要执行的函数。每个线程执行的是相同函数，因此CUDA C是SPMD（单一程序多数据）编程风格的实例

当主机端调用一个核函数，所有的线程被组织为两级结构：

一个核函数由一个 grid 运行，一个 grid 含有多个 blocks，一个 block 内有多个 threads。每个 block 内 threads 的数量都是相同的，且最多为 1024 个。

blockIdx，blockDim，threadIdx

x，y，z三个维度，根据数据维度来组织线程维度，每个维度线程数量应为32的倍数 

### ![image-20260423153632822](D:\proj\LittelWidgets\2GPU编程\大规模并行处理器编程实战-笔记.assets\image-20260423153632822.png)

默认不声明的函数都是host函数,核函数和device函数一般是后加的,需要声明关键字

### 2.6调用核函数

```
#include <stdio.h>
#include <iostream>
__global__ void vecAddKernel(float *A, float *B, float *C, int n) {
    // 每个线程执行同样操作,原来是for循环
    // 现在在每个线程里面就只有加法了
    // 块维度 * 块索引 + 线程索引
    int i = threadIdx.x + blockDim.x * blockIdx.x;
    if (i < n) {
        C[i] = A[i] + B[i];
        // printf("thread %d\n", i);
    }
}
```

```
void vecAdd(float* A, float* B, float* C, int n) {
    float *A_d, *B_d, *C_d;
    int size = n * sizeof(float);

    cudaMalloc((void **) &A_d, size);
    cudaMalloc((void **) &B_d, size);
    cudaMalloc((void **) &C_d, size);

    cudaMemcpy(A_d, A, size, cudaMemcpyHostToDevice);
    cudaMemcpy(B_d, B, size, cudaMemcpyHostToDevice);

    vecAddKernel<<<ceil(n/256.0), 256>>>(A_d, B_d, C_d, n);

    cudaMemcpy(C, C_d, size, cudaMemcpyDeviceToHost);

    cudaFree(A_d);
    cudaFree(B_d);
    cudaFree(C_d);
}
```

上面代码的n表示想要的总线程数目,需要向上取整

vecAddKernel<<<xx, yy>>>(...)

xx表示网格中的块数量,

yy表示块中的线程数量

用vscode复现了,主要了一个main函数

```
int main() {
    int n = 5;
    float a[5] = {1,2,3,4,5};
    float b[5] = {1,1,1,1,1};
    float c[5] = {0,0,0,0,0};
    vecAdd(a, b, c, n);
    for (int i = 0; i < n; i++) {
        std::cout << "rst " << i << " = "<<  c[i] << std::endl;
    }
    return 0;
}
```

得到输出(root@xx#表示终端输入)

```
root@xx#nvcc -o test main.cu
root@xx#./test 
thread 0
thread 1
thread 2
thread 3
thread 4
rst 0 = 2
rst 1 = 3
rst 2 = 4
rst 3 = 5
rst 4 = 6
```

### 2.7编译

CUDA C 的编译运行过程如下所示，首先由 NVCC 将主机代码和设备代码进行分离，主机代码交由主机的 C 编译器进行编译链接，设备代码将被编译为 PTX 的虚拟二进制格式，然后再由设备的 JIT 进行二次编译运行。

![image-20260423163132309](D:\proj\LittelWidgets\2GPU编程\大规模并行处理器编程实战-笔记.assets\image-20260423163132309.png)

### 练习题

1.C

2.

3.

4.

5.

6.

7.

8.

9.

10.

## Chapter3,多维网格和组织

### 3.1多维网格组织

